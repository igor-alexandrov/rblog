%posts
  %ul
    -for post in @posts
      %li
        %h2
          =post.title
        -unless post.announcement.empty?
          =post.announcement
        -else
          =post.body
        %br
        =link_to "Читать полностью", post
        %br
        комментариев:
        =post.comments_count
        =post.created_at.strftime("%d-%B-%Y")
